# Transfer Content

This directory contains the static extraction from the previous WordPress site.

- `source-home.html` is the rendered WordPress homepage captured from `https://neurobodywork.com/`.
- `source-content.json` is the normalized content used to rebuild the React pages.
- `assets/` contains downloaded WordPress media referenced by the source page.
